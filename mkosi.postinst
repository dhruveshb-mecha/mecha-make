#!/bin/bash
set -eux

# switch to graphical target so greetd (display manager) starts on boot
systemctl set-default graphical.target

# enable seat management for Wayland compositors running as non-root
systemctl enable seatd.service

#!/bin/bash
set -eux

# Compile dconf database if the tree exists.
if [ -d /etc/dconf/db ]; then
    dconf update
else
    echo "/etc/dconf/db missing; skipping dconf update"
fi

exit 0
# enable the display manager
# systemctl enable greetd

# switch to graphical target
#systemctl set-default graphical.target
